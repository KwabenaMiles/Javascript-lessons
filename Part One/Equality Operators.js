const age = 18;

if (age === 18) console.log("You're old enough") ///Now, whenever or if block only has one line, we actually don't need two curly braces. 

if (age == 18) console.log("You're old enough")